# Contributing to ZQM-Labs/zqm-supply-chain-scanner

## Development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Testing
```bash
pytest
```

## Commit Messages
Follow conventional commits: `feat:`, `fix:`, `docs:`, `chore:`, etc.
